# kustomize-vpc-cni
Example code to be able to customise the AWS VPC CNI to run different daemonsets

This is useful if you run different workloads with different requirements such as number of IP addresses allocated.
