apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: aws-node-stateless
  namespace: kube-system
spec:
  selector:
    matchLabels:
      k8s-app: aws-node-stateless
  template:
    metadata:
      labels:
        k8s-app: aws-node-stateless

